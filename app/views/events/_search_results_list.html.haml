%ul.nav.nav-list
  - for event in @events do
    %li
      %a{:href => "#", :onclick => "moveEvent(#{event.id})"}
        %div{:id => "#{event.id}"}
          %div
            = event.title
          %div.muted
            = event.date_str
    %li.divider